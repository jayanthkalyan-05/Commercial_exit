{
  "workflow_name": "Engine Data Processing Pipeline",
  "common_folder": "data",
  "copy_output_to_common": false,
  "cleanup_intermediate_files": true,
  "description": "Process multiple Csv_bom_esn_option files through initial pipeline stages",
  "scripts": [
    {
      "name": "Join2",
      "required": true,
      "input_files": ["Csv_bom_esn_option_{base_variant}.csv", "Csv_bom_esn_config.csv"],
      "output": "file2_{option_file_name}.csv",
      "description": "Join the both inbound files - runs per option file",
      "run_mode": "per_option_file"
    },
    {
      "name": "Concatanation",
      "required": true,
      "input_files": ["file2_{option_file_name}.csv"],
      "output": "file3_{option_file_name}.csv",
      "description": "Concatenation of shop order and design config - runs per option file",
      "run_mode": "per_option_file",
      "cleanup_after": true
    },
    {
      "name": "Aggregation",
      "required": true,
      "input_files": ["file3_*.csv"],
      "output": "file4_aggregated.parquet",
      "description": "Aggregate all file3 outputs with distinct Engine_serial count",
      "run_mode": "aggregate_once",
      "output_format": "parquet"
    },
    {
      "name": "UE2",
      "required": true,
      "input_files": ["file4_aggregated.parquet"],
      "output": "file5.parquet",
      "description": "Sort and deduplicate by esn_config_design_config_Shop_order_num - keeps max year",
      "run_mode": "once",
      "output_format": "parquet"
    },
    {
      "name": "Join",
      "required": true,
      "input_files": ["file4_aggregated.parquet", "file5.parquet", "child_part_option.parquet"],
      "output": "file6.csv",
      "description": "Step 1: Join file4 with child_part_option. Step 2: Join result with file5",
      "run_mode": "once"
    },
    {
      "name": "UE3",
      "required": true,
      "input_files": ["file6.csv"],
      "output": "file7.csv",
      "description": "Final processing with Child_Part_Number - runs once",
      "run_mode": "once"
    }
  ],
  "file_patterns": {
    "Csv_bom_esn_option": {
      "pattern": "Csv_bom_esn_option_*.csv",
      "required": true,
      "description": "Multiple BOM ESN Option mapping files"
    }
  },
  "data_files": {
    "Csv_bom_esn_config.csv": {
      "required": true,
      "description": "BOM ESN Configuration file (single master file)"
    },
    "child_part_option.csv": {
      "required": true,
      "description": "Child part to option mapping file (single master file)"
    }
  }
}